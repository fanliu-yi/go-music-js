# Go Music JS v0.17

Go Music JS 是基于 `go-music-dl` 项目开发、运行于 Songloft 插件容器中的 JS 插件。本版本将已确认的 `0.16.1` 测试版固化为正式版，重点完成搜索平台收敛和移动端布局修正，同时保留已验证的歌曲播放链路。

## 更新内容

- 移除顶部 Go Music JS 标识，减少搜索页面占用空间。
- 将搜索类型改为搜索栏左侧下拉菜单，选项为“单曲、歌单、专辑”。
- 搜索源统一保留并按以下顺序显示：酷我、网易云、QQ、酷狗、咪咕、汽水。
- 设置页平台登录统一保留以上六个平台，并按相同顺序显示。
- 清理旧配置中残留的 Bilibili、JOOX、Apple Music、千千音乐、5sing、Jamendo 等平台，避免它们重新出现在搜索源和平台设置中。
- 安卓端搜索源调整为每行 3 个，共两行。
- 安卓端播放控制栏固定在底部并铺满屏幕宽度，移除两侧及底部空隙。
- 保留歌曲搜索、歌单、排行榜、本地曲库、登录、换源、导入、下载、歌词和封面等既有功能。
- 保留汽水 SEO 音源兜底、酷我移动端原生代理和 Range 播放处理。

## 使用说明

1. 在 Songloft 插件管理中安装 `go-music-js-v0.17.jsplugin.zip`。
2. 搜索栏左侧选择“单曲”“歌单”或“专辑”，输入关键词后搜索。
3. 在搜索源区域选择需要使用的平台；设置页可配置对应平台的 Cookie 或扫码登录。
4. 安卓端可直接通过底部播放栏控制当前歌曲。

## 验证

- `node --check static/js/app.js`
- `npm run build`
- `npm run validate`
- 已检查构建包中的入口 HTML、哈希 CSS/JS 资源、版本清单和移动端布局规则。

## 兼容性说明

- 未修改已确认正常的汽水播放、歌词、封面、酷我移动端代理和 Range 播放核心逻辑。
- 本版本不包含未确认的其他平台扩展，也不改变 Songloft 宿主接口契约。
