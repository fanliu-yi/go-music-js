# Go Music JS v0.1.6

Go Music JS 是基于 `go-music-dl` 项目开发、运行于 Songloft 插件系统中的 JS 插件。本次重新打包 v0.1.6，内容回到汽水 SEO 兜底验证版；此前未确认的 UI 重设计不包含在本包中。

## 更新内容

- 汽水歌曲详情继续优先请求原有 `track_v2` 接口。
- 当旧接口请求失败、返回空响应、无效数据或 API 错误时，回退到汽水 SEO `seo_track` 接口，避免因空响应导致播放请求失败。
- 将 SEO 返回的歌曲、播放器和歌词字段适配为插件现有数据结构，保留既有音质选择逻辑。
- 兼容带 `play_auth` 的加密音频和不带 `play_auth` 的明文 M4A；明文音频不再执行不必要的解密。
- 保留原有代理、Range 请求处理，以及搜索、播放、歌词、封面、登录、歌单、排行榜和设置功能。
- 使用此前稳定界面。本次仅修正正式包内容和版本标记，不包含后续 UI 重设计。

## 使用说明

1. 在 Songloft 插件管理中安装 `go-music-js-v0.1.6.jsplugin.zip`。
2. 搜索或打开汽水音乐歌曲后播放。插件先尝试旧歌曲详情接口，遇到异常或空数据时自动回退到 SEO 接口。
3. 如果上游歌曲没有提供歌词，插件无法从音频地址补出歌词，该曲目可能没有歌词显示。

## 验证与边界

- 构建和插件结构校验通过。
- 目标歌曲实测中，旧 `track_v2` 返回空响应；SEO 接口返回匹配歌曲和多档音频信息。
- 最高档 M4A 不带 `play_auth`，走明文播放路径；上游音频 Range 请求返回 HTTP `206`，内容格式为 M4A。
- 已验证 NAS 侧上游接口与音频 Range。宿主插件健康路由需要 Songloft 登录令牌，因此未将该项描述为独立端到端验证通过。
